<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/mainColor"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <ImageView
        android:layout_width="70dp"
        android:layout_height="70dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_margin="10dp"
        android:id="@+id/item_admin_food_image"
        android:scaleType="fitXY"
        android:src="@drawable/hotdog"
        tools:ignore="ContentDescription" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:textColor="@color/white"
        android:textStyle="normal"
        app:layout_constraintBottom_toTopOf="@id/item_admin_food_cost"
        app:layout_constraintTop_toTopOf="@id/item_admin_food_image"
        app:layout_constraintLeft_toRightOf="@id/item_admin_food_image"
        android:text="Hot Dog"
        android:layout_marginStart="10dp"
        android:id="@+id/item_admin_food_name"/>

    <TextView
        android:id="@+id/item_admin_food_cost"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="10000"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="@id/item_admin_food_name"
        app:layout_constraintTop_toBottomOf="@id/item_admin_food_name" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/som"
        android:text="So'm"
        android:textStyle="bold"
        android:textSize="20sp"
        android:textColor="@color/white"
        app:layout_constraintLeft_toRightOf="@id/item_admin_food_cost"
        app:layout_constraintTop_toTopOf="@id/item_admin_food_cost"
        app:layout_constraintBottom_toBottomOf="@id/item_admin_food_cost"
        android:layout_marginStart="10dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>